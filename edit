<a href="{{ url_for('main.student_detail', student_id=student.id) }}"
     class="student-card">

 <a href="{{ url_for('main.delete_student', student_id=student.id) }}"
         class="delete-icon"
         onclick="return confirm('Delete this student?');">
          Delete
      </a>

<a href="{{ url_for('main.add_student') }}" class="add-btn">
    + Add Student
  </a>

